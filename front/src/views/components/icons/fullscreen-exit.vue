<template>
  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
    stroke-linecap="round" stroke-linejoin="round">
    <!-- 前景窗口（完整矩形） -->
    <rect x="3" y="7" width="16" height="12" />
    <!-- 背景窗口（只画右上两条边） -->
    <path d="M7 3h16v12" />
  </svg>
</template>
